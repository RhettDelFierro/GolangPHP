var React = require("react");
var Header = require("./Header");

var Home = React.createClass({
    render: function () {
        return (
            <Header />
        )
    }
});

module.exports = Home;